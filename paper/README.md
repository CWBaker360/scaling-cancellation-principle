# Paper

This directory contains the authoritative preprint pair:

```text
scaling_cancellation_principle.tex
scaling_cancellation_principle.pdf
```

The PDF was compiled directly from the included LaTeX source. The repository
uses clean public filenames while preserving the paper title inside the source.
